#!/bin/sh

puf_lib=PUFLIB_PATH
proj="$1"

if [ -z $proj ] ; then
    echo ERROR, no project file provided  >&2
    exit -1
fi
cat $proj | awk -f "${puf_lib}/config.awk" \
    	        -f "${puf_lib}/make_config.awk"
